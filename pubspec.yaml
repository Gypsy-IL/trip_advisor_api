name: trip_advisor_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  json_annotation: ^4.7.0
  json_serializable: ^6.5.2

dev_dependencies:
  build_runner: ^2.3.1
  lints: ^2.0.0
  test: ^1.16.0